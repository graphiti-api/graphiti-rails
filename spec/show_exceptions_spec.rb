require_relative "rails_helper"

RSpec.describe "context" do
  context "JSON API" do
    it "renders correct format"
  end

  context "additional registered format" do
    it "renders with more details"
  end

  context "other format" do
    it "calls default exceptions app"
  end
end
